# Intelligence Workspace

The `/intelligence` route presents descriptive business findings, performance charts, suggested analyst questions, and links into diagnostic/predictive workflows.

The current page uses a small synthetic preview to demonstrate the presentation layer. The next integration should pass the actual profiled dataset into this workspace instead of duplicating demo rows.
