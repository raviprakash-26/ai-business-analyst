# Intelligence UI Components

- `BusinessCharts` — presentation-level trend and contribution charts.
- `IntelligenceSummary` — descriptive findings from the loaded preview.
- `SuggestedQuestions` — guided entry points for the AI Analyst.
- `InsightActions` — links from description into diagnostic/predictive workflows.

These components intentionally avoid making causal or predictive claims from descriptive preview data.